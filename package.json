{
  "name": "btmc-stripe-webhooks",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "2.6.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "start:scheduled": "wrangler dev --test-scheduled --env preview",
    "deploy": "wrangler publish"
  }
}
